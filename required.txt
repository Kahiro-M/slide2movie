# Windows
# pip install python-pptx gTTS pydub pywin32 pyinstaller

# Linux
# sudo apt update
# sudo apt install -y ffmpeg unoconv libreoffice curl
# curl -fsSL https://raw.githubusercontent.com/VOICEVOX/voicevox/0.25.2/build/installer_linux.sh >tmp_voicevox_installer.sh
# VERSION=0.25.2 NAME=linux-nvidia-appimage bash tmp_voicevox_installer.sh
# python3 -m venv --system-site-packages .env
# source .env/bin/activate
# pip install python-pptx gTTS pydub